apiVersion: v1
kind: Service
metadata:
  name: {{ include "batle-api.fullname" . }}
  labels:
    {{- include "batle-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "batle-api.selectorLabels" . | nindent 4 }}
