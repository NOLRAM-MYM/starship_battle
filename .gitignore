node_modules/
dist/
target/
*.log
.env
.env.local
.DS_Store
.vite/
coverage/
playwright-report/
test-results/
pnpm-lock.yaml

# Saída do /graphify: grafo de conhecimento regenerável a partir do
# código, com cache de AST e embeddings dentro. São ~4 MB que mudam a
# cada análise e não descrevem nada que o próprio repositório já não diga.
graphify-out/
